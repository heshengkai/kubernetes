apiVersion: v1
kind: Service
metadata:
  name: icp-service-platform
  labels:
    name: icp-service-platform
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: icp-service-platform
